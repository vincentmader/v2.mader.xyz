import json
import os

import maderxyz.config


def main():
    pass

    # chat_history = []

    # PATH_TO_EXPORTS = os.path.join(config.PATH_TO_RAW_DATA, 'skype')
    # for export in os.listdir(PATH_TO_EXPORTS):
    #     # TODO: make sure this works even if there are multiple exports,
    #     # i.e. I don't want to return duplicate data
    #     if export in ['.DS_Store']:
    #         continue
    #     path_to_chat_history = os.path.join(
    #         PATH_TO_EXPORTS, export, 'messages.json'
    #     )
    #     with open(path_to_chat_history) as fp:
    #         content = json.load(fp)['conversations']

    #     for conversation in content:
    #         pass
    #         # print(conversation)
    #         # input()

    #         # TODO: write the rest of this method
